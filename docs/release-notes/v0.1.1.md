# skill-sync-sidecar v0.1.1

Release infrastructure and repository metadata polish for the initial public handoff.

## Highlights

- Adds the GitHub Release workflow that builds and uploads wheel, source archive, and git bundle assets.
- Adds MIT license, changelog, security policy, and README install instructions.
- Removes the old pip `in-tree-build` feature flag from the package smoke test.
- Keeps the MVP sync behavior unchanged from `v0.1.0`.

## Assets

- Python wheel: `skill_sync_sidecar-0.1.1-py3-none-any.whl`
- Source archive: `skill-sync-sidecar-0.1.1-source.tar.gz`
- Git bundle: `skill-sync-sidecar-0.1.1.bundle`
